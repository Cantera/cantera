#!/bin/sh

PY_DEMOS = isentropic.py soundSpeeds.py
PYTHON_CMD = @PYTHON_CMD@

run:
	@(for py in $(PY_DEMOS) ; do \
	    echo "running $${py}..."; \
          $(PYTHON_CMD) "$${py}"; \
	done)

clean:
	rm -f *.log *.csv *.xml

# end of file

