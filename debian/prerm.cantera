#!/bin/sh

if [ "$1" = "remove" ]; then
    MODDIR=`python -c "from distutils.sysconfig import *; print get_python_lib()"`
    rm -f $MODDIR/cantera/*.py[c,o]
    rm -f $MODDIR/cantera/*/*.py[c,o]
fi
